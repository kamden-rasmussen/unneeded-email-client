# Runtime data directory — contains all secrets and live config
data/

# Legacy locations (kept in case files exist outside data/)
credentials.json
gmail_token.json
*_token.json
config.yaml
*.db

# Compiled binary
email-agent
